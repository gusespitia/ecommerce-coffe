/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', '127.0.0.1', 'https://res.cloudinary.com', 'res.cloudinary.com'], // Add your backend domain here
    },
  };

export default nextConfig;
